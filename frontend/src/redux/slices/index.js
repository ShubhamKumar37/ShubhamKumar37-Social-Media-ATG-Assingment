import { setSignupData, setToken } from "./authSlice";
import { setDescription, setTitle } from "./postSlice";
import { setAvatar, setUserData } from "./userSlice";

export {
  setSignupData,
  setToken,
  setAvatar,
  setUserData,
  setDescription,
  setTitle,
};
